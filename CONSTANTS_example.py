# Note that this is not a functional file, just an example of the format
# I don't recommend committing your CONSTANTS.py file
from SECRETS import secrets

APP_ID = secrets['app_id']
APP_SECRET = secrets['app_secret']
